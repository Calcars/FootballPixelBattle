import React from "react";
import ReactDOM from "react-dom/client";
import FootballPixelBattle from "./FootballPixelBattle";
import "./style.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FootballPixelBattle />
  </React.StrictMode>
);
